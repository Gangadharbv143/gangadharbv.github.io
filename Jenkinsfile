pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build Successful!'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Successful!'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment Successful!'
            }
        }
    }
}
